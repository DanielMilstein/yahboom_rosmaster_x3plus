planning_scene_monitor_options:
  name: "planning_scene_monitor"
  robot_description: "robot_description"
  joint_state_topic: "/joint_states"
  attached_collision_object_topic: "/moveit_cpp/planning_scene_monitor"
  publish_planning_scene_topic: "/moveit_cpp/publish_planning_scene"
  monitored_planning_scene_topic: "/moveit_cpp/monitored_planning_scene"
  wait_for_initial_state_timeout: 10.0

planning_pipelines:
  pipeline_names: ["ompl"]

plan_request_params:
  planning_attempts: 5
  planning_pipeline: ompl
  max_velocity_scaling_factor: 0.3
  max_acceleration_scaling_factor: 0.3

# Trajectory execution tolerances for the in-process MoveItPy trajectory
# manager. 0.20 rad is wider than the start-state safety check would
# normally use, but in this Gazebo setup the PlanningSceneMonitor lags
# the raw /joint_states topic by 0.05–0.28 rad across step transitions
# (both for the arm and the gripper). The actual safety properties we
# rely on are upstream — IK collision-free goals, table-floor clamp,
# multi-depth feasibility search, and grasp verification — none of which
# depend on this tolerance. The closed-loop gripper close bypasses
# trajectory-execution-manager entirely via _send_gripper_position_direct.
trajectory_execution:
  allowed_execution_duration_scaling: 1.2
  allowed_goal_duration_margin: 0.5
  allowed_start_tolerance: 0.20
  execution_velocity_scaling: 1.0
